<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.MiPrimerAplicacion" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Personaliza aquí -->
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme.MiPrimerAplicacion" parent="Base.Theme.MiPrimerAplicacion" />

    <!-- Tema para la pantalla de carga -->
    <style name="Theme.MyApp.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/mi_logo</item>
        <item name="windowSplashScreenAnimationDuration">0</item>
        <item name="postSplashScreenTheme">@style/Theme.MiPrimerAplicacion</item>
    </style>

</resources>
